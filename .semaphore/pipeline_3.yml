version: v1.0
name: 'Deploy ${{parameters.ENVIRONMENT}}'
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Deploy  ${{parameters.ENVIRONMENT}}'
    task:
      jobs:
        - name: 'Deploy  ${{parameters.ENVIRONMENT}}'
          commands:
            - echo "Deploy to $ENVIRONMENT"
