version: v1.0
name: Publish to App Store
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Publish beta version
    task:
      jobs:
        - name: Publish
          commands:
            - checkout
            - cache restore
            - bundle exec fastlane beta
